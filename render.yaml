services:
  - type: web
    name: effort-analyzer-backend
    env: python
    rootDir: effort-analyzer-backend
    plan: free
    pythonVersion: 3.11.14
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
